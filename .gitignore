# Environments and Secrets (CRITICAL)
.env
.venv
env/
venv/
ENV/

# Python Cache
__pycache__/
*.py[cod]
*$py.class

# Django stuff
*.log
local_settings.py
db.sqlite3
bms_backup.sql
db.sqlite3-journal

# Static and Media files (AWS will handle these dynamically)
staticfiles/
media/

# pipenv 
# (Note: We do NOT ignore Pipfile or Pipfile.lock because AWS App Runner needs them!)
.pipenv/

# IDE and OS files
.vscode/
.idea/
*.swp
.DS_Store
Thumbs.db